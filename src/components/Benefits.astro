---
// Componente para mostrar los beneficios de desarrollar a los colaboradores con estadísticas
---

<section id="benefits" class="py-32 bg-gradient-to-b from-gray-50 to-white relative overflow-hidden">
  <!-- Elementos decorativos de fondo -->
  <div class="absolute inset-0 overflow-hidden opacity-10">
    <div class="absolute top-0 -left-10 w-72 h-72 bg-primary rounded-full mix-blend-multiply filter blur-3xl animate-float"></div>
    <div class="absolute bottom-0 right-0 w-80 h-80 bg-primary rounded-full mix-blend-multiply filter blur-3xl animate-float-delayed"></div>
  </div>

  <div class="container relative z-10">
    <div class="max-w-3xl mx-auto text-center mb-20">
      <h2 class="heading-2 text-gray-900 mb-6">
        Beneficios de Desarrollar a tus Colaboradores
      </h2>
      <p class="body-large text-gray-600 max-w-2xl mx-auto">
        Invierte en el crecimiento de tu equipo y observa resultados tangibles.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-10 max-w-5xl mx-auto">
      <!-- Estadística 1 -->
      <div class="stat-card bg-white rounded-2xl p-10 shadow-xl hover:shadow-2xl transition-all duration-500 text-center transform hover:-translate-y-2 border border-gray-100 relative group">
        <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-primary/5 to-primary/0 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        <div class="absolute -inset-0.5 bg-gradient-to-br from-primary/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 blur-sm transition-opacity duration-500 -z-10"></div>
        
        <div class="stat-icon-container mb-6 mx-auto">
          <div class="w-20 h-20 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-10 h-10 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
              <polyline points="17 6 23 6 23 12"></polyline>
            </svg>
          </div>
        </div>
        
        <div class="relative z-10">
          <div class="text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-primary to-primary-700 mb-4 stat-number">+30%</div>
          <h3 class="text-2xl font-semibold text-gray-900 mb-4">Resultados Empresariales</h3>
          <p class="text-gray-600">Incremento medible en los indicadores clave de rendimiento empresarial</p>
        </div>
      </div>

      <!-- Estadística 2 -->
      <div class="stat-card bg-white rounded-2xl p-10 shadow-xl hover:shadow-2xl transition-all duration-500 text-center transform hover:-translate-y-2 border border-gray-100 relative group">
        <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-primary/5 to-primary/0 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        <div class="absolute -inset-0.5 bg-gradient-to-br from-primary/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 blur-sm transition-opacity duration-500 -z-10"></div>
        
        <div class="stat-icon-container mb-6 mx-auto">
          <div class="w-20 h-20 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-10 h-10 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="8.5" cy="7" r="4"></circle>
              <line x1="18" y1="8" x2="23" y2="13"></line>
              <line x1="23" y1="8" x2="18" y2="13"></line>
            </svg>
          </div>
        </div>
        
        <div class="relative z-10">
          <div class="text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-primary to-primary-700 mb-4 stat-number">-5%</div>
          <h3 class="text-2xl font-semibold text-gray-900 mb-4">Tasa de Rotación</h3>
          <p class="text-gray-600">Reducción significativa en la rotación de personal y costos asociados</p>
        </div>
      </div>

      <!-- Estadística 3 -->
      <div class="stat-card bg-white rounded-2xl p-10 shadow-xl hover:shadow-2xl transition-all duration-500 text-center transform hover:-translate-y-2 border border-gray-100 relative group">
        <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-primary/5 to-primary/0 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        <div class="absolute -inset-0.5 bg-gradient-to-br from-primary/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 blur-sm transition-opacity duration-500 -z-10"></div>
        
        <div class="stat-icon-container mb-6 mx-auto">
          <div class="w-20 h-20 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-10 h-10 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path>
              <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
            </svg>
          </div>
        </div>
        
        <div class="relative z-10">
          <div class="text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-primary to-primary-700 mb-4 stat-number">3.6x</div>
          <h3 class="text-2xl font-semibold text-gray-900 mb-4">Mayor Potencial</h3>
          <p class="text-gray-600">Más posibilidades de que tus colaboradores desarrollen un alto potencial</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .animate-float {
    animation: float 8s ease-in-out infinite;
  }
  
  .animate-float-delayed {
    animation: float 8s ease-in-out 4s infinite;
  }
  
  @keyframes float {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(15px, -15px); }
  }
  
  .animate-pulse-subtle {
    animation: pulse-subtle 3s infinite;
  }
  
  @keyframes pulse-subtle {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
  }
  
  .stat-number {
    opacity: 0;
    transform: translateY(20px);
  }
  
  .stat-card {
    box-shadow: 0 10px 30px -15px rgba(169, 46, 255, 0.15);
  }
</style>

<script>
  // Animación mejorada al hacer scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        // Animación para la tarjeta
        entry.target.classList.add('animate-fade-in-up');
        
        // Animación para el número dentro de la tarjeta
        const numberElement = entry.target.querySelector('.stat-number');
        if (numberElement instanceof HTMLElement) {
          setTimeout(() => {
            numberElement.style.opacity = '1';
            numberElement.style.transform = 'translateY(0)';
            numberElement.style.transition = 'opacity 0.5s ease-out, transform 0.5s ease-out';
          }, 300);
        }
        
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.2 });

  // Elementos a animar
  const statCards = document.querySelectorAll('#benefits .stat-card');
  statCards.forEach((card, index) => {
    if (card instanceof HTMLElement) {
      card.style.opacity = '0';
      card.style.animationDelay = `${index * 200}ms`;
      observer.observe(card);
    }
  });
</script> 