<section id="features" class="py-24 bg-white">
  <div class="container">
    <div class="max-w-3xl mx-auto text-center mb-16">
      <h2 class="heading-2 text-gray-900 mb-6">
        Diseñado para Lugares de Trabajo Modernos
      </h2>
      <p class=" text-md text-gray-600">
        Nuestra plataforma combina tecnología de vanguardia con metodologías de aprendizaje probadas
      </p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      <div class="bg-white text-primary rounded-xl p-8 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
        <div class="icon mb-6">
          <!-- Aquí puedes insertar un icono SVG o de una librería de iconos -->
          <img src="/Trophy.svg" alt="Impulsa el Crecimiento" class="w-12 h-12 items-center ">
        </div>
        <h3 class="text-2xl font-semibold mb-4">Impulsa el Crecimiento</h3>
        <p class="text-lg text-gray-600">
          Transforma el potencial de tu equipo en resultados tangibles, acelerando el desarrollo profesional y el éxito de tu empresa.
        </p>
      </div>

      <div class="bg-white text-primary rounded-xl p-8 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
        <div class="icon mb-6">
          <!-- Aquí puedes insertar un icono SVG o de una librería de iconos -->
          <img src="/Learned.svg" alt="Adapta el Aprendizaje" class="w-12 h-12 items-center ">
        </div>
        <h3 class="text-2xl font-semibold mb-4">Adapta el Aprendizaje</h3>
        <p class="text-lg text-gray-600">
          Personaliza la formación a las necesidades únicas de cada empleado, maximizando la retención y aplicación de conocimientos.
        </p>
      </div>

      <div class="bg-white text-primary rounded-xl p-8 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
        <div class="icon mb-6">
          <!-- Aquí puedes insertar un icono SVG o de una librería de iconos -->
          <img src="/Training.svg" alt="Mide el Impacto" class="w-12 h-12 items-center ">
        </div>
        <h3 class="text-2xl font-semibold mb-4">Mide el Impacto</h3>
        <p class="text-lg text-gray-600">
          Visualiza el progreso en tiempo real y toma decisiones informadas para optimizar el rendimiento de tu equipo y tu inversión.
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  // Animación al hacer scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate-fade-in-up');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  // Elementos a animar
  const featureCards = document.querySelectorAll('#features .grid > div');
  featureCards.forEach((card, index) => {
    if (card instanceof HTMLElement) {
      card.style.opacity = '0';
      card.style.animationDelay = `${index * 150}ms`;
      observer.observe(card);
    }
  });
</script> 